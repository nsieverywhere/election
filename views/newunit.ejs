<%- include('partials/header'); -%>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Unit</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

</head>
<body>
    <div class="custom-container">
        <header>
            <%- include('partials/navbar'); -%>
        </header>
        <script>
            // Function to hide the message after a certain duration
            function hideMessage() {
              var messageElement = document.getElementById('message');
              messageElement.style.display = 'none';
            }
        
            // Set a timeout to hide the message after 3 seconds (3000 milliseconds)
            setTimeout(hideMessage, 2000);
          </script>
       
        <main class="content">
            <h2 class="pollingunitheading">Enter a new polling Unit data</h2>
            <% if (message) { %>
                <p id="message" class="message"><%= message %></p>
              <% } %>
            <form id="pollingUnitForm" action="/newunit" method="POST">
                <div class="form-group">
                  <label for="pollingUnitId">Polling Unit ID</label>
                  <input type="text" class="form-control" id="pollingUnitId" name="polling_unit_id">
                </div>
              
                <div class="form-group">
                  <label for="wardId">Ward ID</label>
                  <input type="text" class="form-control" id="wardId" name="ward_id">
                </div>
              
                <div class="form-group">
                  <label for="lgaId">LGA ID</label>
                  <input type="text" class="form-control" id="lgaId" name="lga_id">
                </div>
              
                <div class="form-group">
                  <label for="pollingUnitNumber">Polling Unit Number</label>
                  <input type="text" class="form-control" id="pollingUnitNumber" name="polling_unit_number">
                </div>
              
                <div class="form-group">
                  <label for="pollingUnitName">Polling Unit Name</label>
                  <input type="text" class="form-control" id="pollingUnitName" name="polling_unit_name">
                </div>
              
                <div class="form-group">
                  <label for="pollingUnitDescription">Polling Unit Description</label>
                  <textarea class="form-control" id="pollingUnitDescription" name="polling_unit_description"></textarea>
                </div>
              
                <div class="form-group">
                  <label for="latitude">Latitude</label>
                  <input type="text" class="form-control" id="latitude" name="lat">
                </div>
              
                <div class="form-group">
                  <label for="longitude">Longitude</label>
                  <input type="text" class="form-control" id="longitude" name="long">
                </div>
              
                <div class="form-group">
                  <label for="enteredByUser">Entered by User</label>
                  <input type="text" class="form-control" id="enteredByUser" name="entered_by_user">
                </div>
              
                <div class="form-group">
                  <label for="dateEntered">Date Entered</label>
                  <input type="datetime-local" class="form-control" id="dateEntered" name="date_entered">
                </div>
              
                <div class="form-group">
                  <label for="userIpAddress">User IP Address</label>
                  <input type="text" class="form-control" id="userIpAddress" name="user_ip_address">
                </div>
              
                <button type="submit" class="btn btn-primary">Submit</button>
              </form>
              

           

            <!-- <form id="myForm" class="lgaform">
                <input type="text" name="lga_id">
                <button type="submit" class="dropdown-item lgabutton">Submit</button>
              </form>
              <script>
                const form = document.getElementById('myForm');
              
                form.addEventListener('submit', function(event) {
                  event.preventDefault();
              
                  const formData = new FormData(form);
              
                  const jsonData = {};
                  formData.forEach((value, key) => {
                    jsonData[key] = value;
                  });
              
                  // Send the data using Axios
                  axios.post('/newunit', jsonData)
                    .then(response => {
                      // Handle the response from the backend
                      console.log(response.data);
                    })
                    .catch(error => {
                      // Handle any errors that occurred during the request
                      console.error(error);
                    });
                });
              </script> -->
          </main>
    </div>

<%- include('partials/footer'); -%>